[package]
name = "ecsdk_app"
version = "0.1.0"
edition = "2024"

[dependencies]
ecsdk_core = { workspace = true }
bevy = { workspace = true }
tokio = { workspace = true, features = ["rt", "sync", "time"] }
